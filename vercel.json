{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "web/dist",
  "installCommand": "cd web && npm install",
  "rootDirectory": ".",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
